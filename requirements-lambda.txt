# Extra dependencies needed only inside the Lambda container image.
#
# The mcr.microsoft.com/playwright/python base image pre-downloads the
# matching Chromium browser binaries, but does NOT reliably leave the
# `playwright` Python package importable from wherever AWS's Lambda Runtime
# Interface Client ends up running Python — pin it explicitly here, at the
# exact version matching the Dockerfile's base image tag, so it's guaranteed
# installed into the same environment the handler actually runs in. This
# does not re-download the browser binaries (those come from the base
# image); it only (re)installs the Python API package.
playwright==1.62.0
awslambdaric==2.2.1
# Python's zoneinfo module needs the IANA timezone database; this image
# doesn't ship it at the OS level, so ZoneInfo("America/Toronto") fails
# without this pure-Python fallback copy of the same database.
tzdata
