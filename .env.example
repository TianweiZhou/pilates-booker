# Copy to .env for local testing (never commit .env)
PILATES_EMAIL=zhou_tianwei@outlook.com
PILATES_PASSWORD=your-password-here

# Local testing knobs
DRY_RUN=true            # stop before the final booking click
WAIT_FOR_RELEASE=false  # don't wait for Thursday 11:00 ET
HEADLESS=false          # watch the browser work
